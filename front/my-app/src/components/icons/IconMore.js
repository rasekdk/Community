import { ReactComponent as IconMoreSvg } from './svg/IconMore.svg';

const IconMore = ({ className, onClick }) => (
  <IconMoreSvg className={className} onClick={onClick} />
);

export default IconMore;

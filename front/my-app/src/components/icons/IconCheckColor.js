import { ReactComponent as IconCheckColorSvg } from './svg/IconCheckColor.svg';

const IconCheckColor = ({ className, onClick }) => <IconCheckColorSvg className={className} onClick={onClick} />;

export default IconCheckColor;

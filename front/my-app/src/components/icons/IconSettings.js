import { ReactComponent as IconSettingsSvg } from './svg/IconSettings.svg';

const IconSettings = ({ className, onClick }) => <IconSettingsSvg className={className} onClick={onClick} />;

export default IconSettings;

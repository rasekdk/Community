import { ReactComponent as IconCrossSvg } from './svg/IconCross.svg';

const IconCross = ({ className, onClick }) => (
  <IconCrossSvg className={className} onClick={onClick} />
);

export default IconCross;

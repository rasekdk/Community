import { ReactComponent as IconDownSvg } from './svg/IconDown.svg';

const IconDown = ({ className, onClick }) => (
  <IconDownSvg className={className} onClick={onClick} />
);

export default IconDown;

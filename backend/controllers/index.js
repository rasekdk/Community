'use strict';

const usersController = require('./userController');
const threadController = require('./threadController');
const communityController = require('./communityControllers');

module.exports = {
  usersController,
  threadController,
  communityController,
};
